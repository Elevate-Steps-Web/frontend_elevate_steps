import { Layout } from '../../components/Layout';

export default function WhoWeAre({ global }) {
  return (
    <Layout global={global} currentPage="Who We Are">
      <h1>We are Elevate!</h1>
    </Layout>
  );
}
